#include "main.h"
#include <stdio.h>

/**
* mul - function that multiplies two numbers
* @a: number to operate with
* @b: number to operate with
* Return: 1 or 0
*/

int mul(int a, int b)
{
return (a * b);
}
