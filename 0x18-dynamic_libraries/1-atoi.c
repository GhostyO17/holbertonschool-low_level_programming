#include "main.h"

/**
 * print_array - print array
 * @a: array input
 * @n: array data []
 *
 * return bad
 */

int _atoi(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
_putchar(s[i]);
}
return (0);
}
